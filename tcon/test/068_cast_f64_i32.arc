pub fn main() i32 {
    let mut f: f64= 9.99;
    let mut i: i32= (i32)f;
    if (i != 9) { return 1; }
    let mut g: f64= -3.7;
    let mut j: i32= (i32)g;
    if (j != -3) { return 2; }
    return 0;
}
